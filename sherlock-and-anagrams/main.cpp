#include <iostream>
#include <cstring>

#ifdef ALGO_DEBUG
#include "../test/debug.cpp"
#else

#define TRACE(message)
#define TRACE_LINE(message)
#define ASSERT(expr)

#endif

int main() {
  int T;
  std::cin >> T;
  for(int t = 0; t < T; ++t) {
    
  }
  return 0;
}
